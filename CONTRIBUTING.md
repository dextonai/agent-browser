# Contributing to agent-browser

First off, thanks for taking the time to contribute!

## Setting up the dev environment

1. Clone the repository:
   ```bash
   git clone https://github.com/dextonai/agent-browser.git
   cd agent-browser
   ```
2. Install the dependencies:
   ```bash
   npm install
   ```
3. Run the tests to ensure everything is working:
   ```bash
   npm run test
   ```

## Coding style guidelines

- Follow the existing formatting and conventions in the codebase.
- Use TypeScript and ensure strict type checking passes.
- Run linting before submitting your code.
- Write
